import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    button: {
        paddingStart: 20,
        paddingEnd: 20,
        paddingTop: 10,
        paddingBottom: 10,
        borderRadius: 10,
        shadowColor: '#000',
        shadowRadius: 25,
        justifyContent: 'center',
        alignItems: 'center',
        borderWidth: 6,
        
    },

    title: {
        fontSize: 20,
        fontWeight: 'bold'
    }
})